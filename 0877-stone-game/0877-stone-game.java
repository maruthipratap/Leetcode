class Solution {
    public boolean stoneGame(int[] piles) {
        return true;
        /*int l=0,r=piles.length-1;
        int alice=0,bob=0;
        while(l<r){
            if(piles[l]<piles[r]){
                alice=piles[r];
                bob=piles[l];
            }else{
                alice=piles[l];
                bob=piles[r];
            }
            l++;
            r--;
        }*/
    }
}